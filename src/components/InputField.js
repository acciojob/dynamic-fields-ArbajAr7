// import React from 'react';

// const InputField = (props) => {
//   const { divid, nameid, ageid, submitbtn, removeField } = props;

//   // Function to handle form submission and prevent default behavior
//   const handleSubmit = (e) => {
//     e.preventDefault();
//   };

//   return (
//     <form id={divid} onSubmit={handleSubmit}>
//       <input id={nameid} type='text' placeholder='Sam'></input>
//       <input id={ageid} type='text' placeholder='Age'></input>
//       <button id={submitbtn} type="button" onClick={() => removeField(divid)}>Remove</button>
//     </form>
//   );
// }

// export default InputField;